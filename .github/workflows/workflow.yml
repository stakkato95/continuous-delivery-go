name: main-worklfow

env: {}

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  # Run all unit tests.
  run-tests:
    runs-on: ubuntu-latest

    # Service containers to run with `container-job`
    services:
      # Label used to access the service container
      postgres:
        # Docker Hub image
        image: postgres
        # Provide the password for postgres
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: test
        ports:
          - 5432:5432

    steps:
      # Check out the pull request's source code.
      - name: Check out source code
        uses: actions/checkout@v2

      # Install Go.
      - name: Set up Go
        uses: actions/setup-go@v2-beta
        with:
          go-version: "1.18" # The Go version to download and use.
      
      - name: Print Go version
        run: go version

      # Run unit tests.
      - name: Run unit tests
        run: make test
  
  # docker-push-image:
  #   runs-on: ubuntu-latest
  #   steps:
  #     # Check out the pull request's source code.
  #     - name: Check out source code
  #       uses: actions/checkout@v2

  #     # Install Go.
  #     - name: Set up Go
  #       uses: actions/setup-go@v2-beta
  #       with:
  #         go-version: "1.18" # The Go version to download and use.
      
  #     - name: Print Go version
  #       run: go version
      
  #     - name: build-image
  #       run: make docker-push-image